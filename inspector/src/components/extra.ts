import { Box } from "@jay-js/elements";

export function Extra() {
	return Box({
		className: "p-4 bg-yellow-100 border-l-4 border-yellow-500 text-yellow-700",
		children: "Extra Component Loaded!",
	})
}